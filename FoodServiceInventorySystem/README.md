# Food Service Inventory Management System

## Run in VS Code
1. Install JDK 21 or newer and Maven.
2. Install the VS Code Extension Pack for Java.
3. Open this project folder in VS Code.
4. Open Terminal > New Terminal.
5. Run:

   mvn clean compile
   mvn exec:java

The SQLite database file is created automatically.
